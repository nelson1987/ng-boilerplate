# ng-boilerplate
Create Boilerplate to fast websites em Angular
```sh
npm -v
mkdir src
cd src
npm install -g @angular/cli
ng version
ng new --no-standalone sistema 
cd sistema
ng s
#http://localhost:4200

```